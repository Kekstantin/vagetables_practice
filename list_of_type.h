#pragma once
#include <map>
#include <random>

#define mp std::make_pair
//std::map<std::pair<float, float>, >

using coord_ = float;
using speed_ = float;

enum type_ { static_, dynamic_ };